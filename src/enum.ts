export enum Collections {
  users = "users",
}

export enum OperationStatus {
  active = "active",
  inactive = "inactive",
}

export enum Status {
  active = "active",
  inactive = "inactive",
  deleted = "deleted",
}

export enum Role {
  admin = "admin",
  shop = "shop",
  customer = "customer",
}
