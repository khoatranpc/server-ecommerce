export enum Collections {
  users = "users",
  shops = "shops",
  categories = "categories",
  products = "products",
}

export enum OperationStatus {
  active = "active",
  inactive = "inactive",
}

export enum Status {
  active = "active",
  inactive = "inactive",
  pending = "pending",
  deleted = "deleted",
}

export enum Role {
  admin = "admin",
  shop = "shop",
  customer = "customer",
}

export enum ServicePackage {
  trial = "trial",
  basic = "basic",
  medium = "premium",
}
